#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	int n, t = 0, i;
	cout << "n = ";
	cin >> n;
	for (i = 2; i <= sqrt(n); i++)
		if (n % i == 0)
			t++;
	if (t > 0)
		cout << "composite";
	else
		cout << "Prime";
		return 0;
}
